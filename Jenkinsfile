pipeline {
  agent any
    stages {
       stage('build'){
       steps {
                sh 'echo "restarting all" '  
                sh '''
                    echo "Multi line work too"
                    ls -alh   
                   '''
                 }
             }
        }
    }
